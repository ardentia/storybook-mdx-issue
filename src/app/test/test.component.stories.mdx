import { Meta, Story, Canvas } from "@storybook/addon-docs";
import { TestComponent } from "./test.component";

<Meta title="Preview/Test" component={TestComponent} />

# TestComponent

Test component running inside `MDX` and Canvas with Storybook 6.4 alpha

export const Template = (args) => ({ props: args });

<Canvas withToolbar>
  <Story name="test-component-with-canvas">{Template.bind({})}</Story>
</Canvas>

## Another Test

Test component running inside `MDX` without `Canvas` block with Storybook 6.4 alpha

<Story name="test-component-without-canvas">{Template.bind({})}</Story>
